# 樹說（TreeSay）

單人本機、無登入的療癒日記 Web App。像傳訊息一樣把想法丟給「樹」，AI 整理成日記並溫暖回覆。日記存在自己的磁碟上，沒有帳號、沒有其他使用者。

命名「樹說」取「訴說」諧音——把心事說給樹聽。目錄與 DB 皆以 `treesay` 為名。

## 核心定位

**一個安靜聽你說、替你把一天收好的傾聽者。** 目標使用者是一個人埋頭做東西的開發者，核心矛盾是「做了很多，但說不出今天做了什麼」——白天把碎片隨手丟給樹，晚上長成一篇日記，說不出形狀的一天就被收好了。樹知道的，就是使用者親口告訴它的；「本機」的意義是儲存（日記在自己的磁碟、沒有帳號），不是監看。

**任何把一天變成可比較數字的設計（趨勢圖、字數、時數、連續天數）都是反目標**，一旦加上去，這個 App 就變成它最想避開的那種工具。

每個介面決策定案前問一次：
> 「一個今天很累、覺得自己什麼都沒做完的人打開這個畫面，會覺得被接住，還是被檢查？」

答案必須是「被接住」。詳見 `docs/plans/2026-07-24-treesay-uiux-positioning.md`。

## 技術棧與環境

- 後端：FastAPI（uvicorn，僅綁 `127.0.0.1`）＋ SQLAlchemy ＋ SQLite
- 前端：Vue 3 ＋ Vite ＋ TypeScript ＋ Pinia（create-vue 鷹架）
- AI：本機 `claude -p --output-format json`（走 Claude 訂閱，async subprocess，timeout 240s）
- Python 環境：一個獨立的虛擬環境（venv、pyenv 都行），不綁環境名稱

## 目錄結構

```
treesay/
├── backend/
│   ├── main.py        # FastAPI 路由
│   ├── ai.py          # claude CLI 封裝（subprocess + JSON 解析 + 重試 + 用途檔次）
│   ├── models.py      # SQLAlchemy 模型、切日邏輯、資料目錄
│   ├── prompts.py     # 所有 prompt 模板（繁中、療癒語氣）
│   ├── questions.py   # 今日問題內建題庫（依日期定題，不耗 AI）
│   └── tests/         # pytest（mock subprocess，不真呼叫 claude）
├── frontend/          # Vue 3 + Vite + TS + Pinia
└── docs/plans/        # 設計文件
```

日記與照片不在 repo 裡：預設落在 `~/.treesay/`（`treesay.db` ＋ `photos/`），
可用 `TREESAY_DATA_DIR` 覆寫。

## 開發規範

- **分段 commit**：實作時按邏輯單元分段提交，一個 commit 只做一件事，避免把多個不相關的變更混在同一次提交。
- **套件版本盡量使用最新版**：安裝相依套件（Python、npm）時優先採用最新穩定版，不釘死在舊版本。
- commit 訊息使用繁體中文 Conventional Commits 格式（如 `feat:`、`fix:`、`docs:`、`chore:`）。
- 通用準則：先思考再動手、簡單優先、外科手術式修改（只改必要的地方）、以目標驅動並反覆驗證。

## 關鍵實作注意

- **AI 輸出是雙層 JSON**：`--output-format json` 回的是 CLI envelope（`{"type":"result","subtype":...,"result":"..."}`）。先解析 envelope 檢查 `subtype` 是否 success，再取 `result` 字串剝除 code fence 後 `json.loads`。任一層解析失敗重試 1 次，再失敗回 502。
- **互動節奏採結算式，逐則回應（方案 B）不做**：白天只收集訊息不呼叫 AI，按「種下今天的樹」才一次生成 `{diary, emotion, tree_reply}`。逐則回應不是「還沒做」而是「不做」——白天零回應壓力是定位的一部分，逐則回應會把樹變成又一個等你回訊息的對象，AI 呼叫也從一天一次變 N 次。不要為它在 `messages` 加欄位（例如 `tree_reaction`）。
- **切日點為本地時間凌晨 4:00**：之前的訊息歸前一天；所有 datetime 存本地時間。
- **不做 git 軌跡**：不讀使用者 repo 的 commit 當日記素材。白名單（逐 repo 列舉）現實中沒有人會維護——開發者的 repo 多且一直長新的，清單一過期功能就形同虛設；放寬成目錄掃描（指一個上層目錄自動找 repo）一樣不做。而且 prompt 必須約束「commit 不是使用者說的話」（否則「不評價」破功），約束完它幾乎影響不了輸出，是一個永遠不兌現的承諾。樹知道的就是使用者親口說的，「本機」的意義只有儲存——不要加任何自動讀取使用者工作痕跡的素材來源。
- **不做假社交**：不讓 AI 扮演匿名的陌生人回信。這種功能的全部價值在於「真的有人讀了我的心事」，換成 AI 之後只剩下同一個模型用不同語氣回你三次，使用者一旦意識到就失效。
- **不做主題選擇**：「今天想聚焦什麼」——早上從幾個主題挑一個、種樹時當整理方向——不要做。它和今日問題做同一件事（給今天一個溫柔的形狀），卻要使用者做選擇，會是全 App 唯一的「設定」，與零門檻丟訊息逆向；且主題早上選、日記晚上才生成，兩者脫節時硬扣主題的日記比沒有更傷。想給一天形狀，往 `questions.py` 加題目就好，不要加需要使用者選擇的機制。
- **週報 lazy 生成**：僅已結束的週可生成並快取；當週顯示「這週還在生長中」。
- **補種窗口刻意很短（`BACKFILL_WINDOW_DAYS = 2`）**：忘記按種樹的那天，48 小時內（昨天、前天）能從森林詳情頁補種，之後過期——訊息仍讀得到，只是不再長樹。兩個理由都不能放寬：①任何一天都補得回來的話，森林就從「發生過的紀錄」變成可以填滿的成果，空格成了待辦事項，正是反目標；②窗口一旦跨週就會撞上週報快取——週報只生成已結束的週且永久快取，補種晚於週報生成，那天會永遠不在週報裡（`test_window_stays_shorter_than_a_week` 釘住這條）。補種的 prompt 帶 `days_ago`，樹的回覆要認得時間差，但不能責怪使用者沒準時回來。
- **未種下的日子必須看得見**：森林 `/api/days` 會回傳有訊息但未種的天（`status` 欄位），詳情頁兩種狀態都顯示訊息。若只回 planted，那天在月曆上就跟從沒打開過 App 一樣，說過的話被 App 吞掉——使用者送出訊息時期待的是晚點被回應，永久沉默等於違約。但呈現一律低調：淡土壤痕跡、不標「未完成」、不計數。
- **訊息可改可收回，但只到種樹前**：修改（`PATCH /api/messages/{id}`）與收回共用 `editable_message` 一條界線——今天、`status == collecting`。打錯字改不了會讓人送出前多一分猶豫，而「要想好再說」正是這個 App 想拿掉的壓力；種下之後日記是從這些話長出來的，事後改素材會讓日記對不上，那天的錯字也是那天的一部分。不記錄編輯痕跡（沒有 `edited_at`、不標「已編輯」）：自己的日記沒有另一個人需要知道你改過什麼。
- **記憶只用來理解與關心，不對帳**：`memories` 表是樹長期記得的、關於使用者的事。抽取搭種樹的便車（plant JSON 多回 `memory` 欄位，不另外呼叫 AI）；注入 prompt 必須帶「不追蹤進度、不對帳、不主動翻舊的難過」的界線——拿掉它樹就從傾聽者變成查帳的。近期日記只注入目標日往前 7 天內最多 3 篇，補種以那一天為基準往回看，不能引用未來的日記。週報是記憶唯一的瘦身時機（合併重複、放下過時；AI 回空清單視為可疑，不套用——樹不該一次忘掉所有事）。每一條記憶使用者在「記憶」頁看得到、刪得掉，刪了不會復活——看不見的記憶比沒有記憶更可怕。
- **情緒固定 7 種枚舉**：`happy / calm / excited / tired / sad / anxious / angry`，枚舉外的值 fallback 為 `calm`。
- **錯誤以療癒文案呈現**：AI 失敗時顯示「樹睡著了，等等再來 🌙」，不裸露技術錯誤。
- **「本機」只涵蓋儲存，不涵蓋 AI**：日記存在自己的磁碟、沒有帳號、沒有其他使用者看得到，但種樹與週報的內容會經由 `claude -p` 送到 Anthropic。文案與文件一律不得出現「無雲端」「不會離開這台電腦」這類說法——宣稱與事實有落差，對一個賣安全感的產品是最傷的事。
- **AI 的工具權限是排他白名單**：日記內容原樣進 prompt，等同不可信輸入。`ai.py` 用 `--tools` 列舉可用工具（預設全關，看照片才開 `Read`）＋ `--setting-sources ""` 不繼承使用者放行規則。不要改用 `--permission-mode` 收斂——實測 `-p` 無頭模式下它擋不住工具。

## 測試（成功標準）

1. `ai.py` 單元測試：JSON 解析各情境（envelope、error subtype、code fence、壞 JSON、枚舉外情緒），不真呼叫 claude。
2. API 整合測試（pytest ＋ mock subprocess）：丟訊息→種樹→日記入庫；凌晨 4:00 前訊息歸前一天；`planting` 中重複種樹被擋；週報快取命中。

## 設計文件

- `docs/plans/2026-07-24-treesay-design.md`——完整設計（資料模型、API、AI 呼叫、roadmap）
- `docs/plans/2026-07-24-treesay-uiux-positioning.md`——UI/UX 定位與反目標
- `design-reference/`——UI/UX 頁面設計稿（Claude Design 匯出的 HTML/CSS 原型，git 忽略、本機參考）。實作前端視覺時以此為準，先讀 `design-reference/project/TreeSay 網頁版.dc.html` 並像素級重現，不照抄原型內部結構。
